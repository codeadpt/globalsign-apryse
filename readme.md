# With Docker Compose

Start container:

`./dc.sh up -d`

Install dependencies:

`./node.sh npm install`

Run script:

`./node.sh node index.js`

# Without Docker Compose

Install dependencies:

`npm install`

Run script:

`node index.js`